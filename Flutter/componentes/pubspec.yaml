name: Componentes
description: Aplicacion de componentes de Flutter, desarrollada por cerbero.dev.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  flutter_localizations:
    sdk: flutter


  cupertino_icons: ^0.1.2

dev_dependencies:
  
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.7.2"

flutter_icons:
  android: true 
  ios: true
  image_path: "assets/cerberodev.png"


flutter:

  uses-material-design: true

  assets:
    - data/menu_opts.json
    - assets/

 